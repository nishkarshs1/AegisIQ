version: '3.8'

services:
  aegisiq:
    build:
      context: .
      dockerfile: Dockerfile
    image: aegisiq:latest
    container_name: aegisiq-platform
    ports:
      - "8000:8000"
      - "8501:8501"
    environment:
      - BACKEND_URL=http://localhost:8000
    restart: unless-stopped
